# Copy to .env and set a real POSTGRES_PASSWORD — docker-compose.yml requires it.
# DATABASE_URL and VITE_API_URL are derived from it and API_PORT, so don't set them here.

POSTGRES_PASSWORD=changeme
